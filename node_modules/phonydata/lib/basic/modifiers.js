"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.modifiers = void 0;
const __1 = require("..");
function modifiers() {
    (0, __1.define)('capitalize', (str) => str.toString().toUpperCase());
    (0, __1.define)('capitalizeFirst', (str) => {
        const s = str.toString();
        return s.charAt(0).toUpperCase() + s.substring(1);
    });
    (0, __1.define)('capitalizeTitle', (str) => str.toString().replace(/(^|[^\w])\w/g, match => match.toUpperCase()));
    (0, __1.define)('toJson', (thing) => JSON.stringify(thing));
    (0, __1.define)('toString', (thing) => thing.toString());
}
exports.modifiers = modifiers;
